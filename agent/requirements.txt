anthropic>=0.39.0
fastapi>=0.115.0
uvicorn>=0.32.0
requests>=2.32.0
python-dotenv>=1.0.0
langgraph>=0.2.0
chromadb>=0.5.0
pydantic>=2.0
pymupdf>=1.24.0
reportlab>=4.0
# Reranker — Cohere is the primary backend (uses COHERE_API_KEY when
# present), sentence-transformers is the local fallback.
cohere>=5.0
sentence-transformers>=3.0
# CI test
